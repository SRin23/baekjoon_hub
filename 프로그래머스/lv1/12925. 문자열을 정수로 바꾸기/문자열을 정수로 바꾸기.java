class Solution {
    public int solution(String s) {
        int answer = 0;
        // boolean oper = true;
        // if(s.charAt(0)=='+'||s.charAt(0)=='-'){
        //     if(s.charAt(0)=='-'){
        //         oper = false;
        //     }
        //     s = s.substring(1);
        // }
        // answer = Integer.parseInt(s);
        // if(!oper) answer = -answer; 
        
        answer = Integer.parseInt(s);
        return answer;
    }
}